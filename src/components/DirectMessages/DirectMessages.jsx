import MessageGroup from "../MessageGroup";

const DirectMessages = () => {
  return (
    <div className="direct-messages-component">
      <MessageGroup messageGroupSpan={"Direct Messages"} />
    </div>
  );
};

export default DirectMessages;
